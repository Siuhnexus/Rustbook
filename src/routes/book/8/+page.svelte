<main>
 <h1 id="enums-and-pattern-matching">
  <a class="header" href="#" onclick={() => {
    // @ts-ignore
    window.externallink("#enums-and-pattern-matching")
}}>
   Enums and Pattern Matching
  </a>
 </h1>
 <p>
  In this chapter, we’ll look at
  <em>
   enumerations
  </em>
  , also referred to as
  <em>
   enums
  </em>
  .
Enums allow you to define a type by enumerating its possible
  <em>
   variants
  </em>
  . First
we’ll define and use an enum to show how an enum can encode meaning along with
data. Next, we’ll explore a particularly useful enum, called
  <code>
   Option
  </code>
  , which
expresses that a value can be either something or nothing. Then we’ll look at
how pattern matching in the
  <code>
   match
  </code>
  expression makes it easy to run different
code for different values of an enum. Finally, we’ll cover how the
  <code>
   if let
  </code>
  construct is another convenient and concise idiom available to handle enums in
your code.
 </p>
</main>
